import * as ActionTypes from './actionTypes';

const WelcomePanelState = {

};

const reducer = function(state = WelcomePanelState,action){
    switch (action.type){
        default:
            return state;
    }
};

export default reducer;